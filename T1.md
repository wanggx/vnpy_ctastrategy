# vn.py CTA/Backtester 支持 A股 T+1 方案

## 目标

在 `vnpy_ctastrategy` 和 `vnpy_ctabacktester` 中增加 A股 T+1 支持，同时保持原有 T+0 行为完全兼容。

默认行为：

- `T+1` 开关默认关闭。
- 关闭时，原有 CTA 策略、回测、参数优化逻辑不变。
- 开启时，只考虑 A股做多。
- 不考虑多个策略交易同一个标的。
- 不支持做空，遇到 `short/cover` 只拒绝并打印日志。
- 买入和卖出数量都按 100 股整数倍处理。
- 实盘通过 `vnpy_xt`，持续使用真实账号持仓数据同步可卖仓。
- 回测默认初始持仓为 0。

## 核心规则

T+1 模式下：

- 当天买入的仓位不能当天卖出。
- 昨日持仓或真实账号可用仓可以卖出。
- 卖出委托不能超过当前可卖仓。
- 做空相关委托直接拒绝。
- 委托数量必须是 100 的整数倍。

## 实盘与回测一致性原则

实盘和回测保持一致的是策略看到的仓位抽象、下单校验和订单冻结/释放规则；不同的是仓位数据来源。

统一仓位口径：

```text
pos = 当前总持仓
yd_pos = 当前总可卖仓
td_pos = 今日买入不可卖仓
local_sell_frozen = 本策略活跃卖单占用仓
available = yd_pos - local_sell_frozen
```

策略和引擎的卖出校验都只看：

```python
available = max(strategy.yd_pos - strategy.local_sell_frozen, 0)
```

实盘和回测的差异仅在更新来源：

| 场景 | 实盘 | 回测 |
|---|---|---|
| 初始持仓 | 来自 `vnpy_xt` 真实持仓 | 默认 0 |
| 买入成交 | 成交事件先本地更新，持仓事件再覆盖纠偏 | 回测成交后立即更新 |
| 卖出成交 | 成交事件先本地更新，持仓事件再覆盖纠偏 | 回测成交后立即更新 |
| 跨日结转 | 以真实持仓同步为准 | 日期变化时 `yd_pos = pos` |
| 可卖仓 | 来自 `PositionData.yd_volume/can_use_volume` | 由回测成交和跨日结转维护 |
| 冻结 | 本地临时防重复卖 | 本地模拟活跃卖单冻结 |

实盘推荐采用“事件先本地更新，持仓事件再覆盖”的方式：

```text
卖单发出成功：
    local_sell_frozen += order.volume

卖出成交：
    pos -= traded_increment
    yd_pos -= traded_increment
    local_sell_frozen -= traded_increment

买入成交：
    pos += traded_increment
    td_pos += traded_increment

撤单/拒单：
    local_sell_frozen -= remaining_volume

收到持仓事件：
    pos = position.volume
    yd_pos = position.yd_volume
    td_pos = max(pos - yd_pos, 0)
```

注意：收到持仓事件时不要无条件清空 `local_sell_frozen`，除非已经确认本策略活跃卖单全部终态。否则在持仓事件刷新和订单事件到达的短窗口内，可能重复卖出。

如果真实账号的 `position.yd_volume` 已经扣除了未成交卖单冻结，再继续扣 `local_sell_frozen` 会偏保守。当前方案接受这种短时间保守偏差，优先避免超卖。订单终态或下一轮持仓同步后会逐步纠偏。

## 涉及模块

### vnpy_ctastrategy

主要修改：

- `vnpy_ctastrategy/template.py`
- `vnpy_ctastrategy/engine.py`
- `vnpy_ctastrategy/backtesting.py`

### vnpy_ctabacktester

主要修改：

- `vnpy_ctabacktester/engine.py`
- `vnpy_ctabacktester/ui/widget.py`

## 持仓相关变量

### CtaTemplate 中建议增加

```python
t1: bool = False

yd_pos: float = 0
td_pos: float = 0
local_sell_frozen: float = 0

position_synced: bool = False
position_sync_time: str = ""
```

### 变量含义

| 变量名 | 含义 |
|---|---|
| `t1` | 是否启用 T+1/A股模式 |
| `pos` | 策略净持仓，沿用原 vn.py 变量 |
| `yd_pos` | T+1 总可卖仓，实盘来自真实账号可用仓，回测由跨日结转和成交维护 |
| `td_pos` | 今日买入持仓 |
| `local_sell_frozen` | 本策略活跃卖单占用的可卖仓数量 |
| `position_synced` | 是否已经收到过真实持仓同步 |
| `position_sync_time` | 最近一次持仓同步时间 |

实际还可以继续新卖出的数量：

```python
available = max(strategy.yd_pos - strategy.local_sell_frozen, 0)
```

其中：

- `yd_pos` 不随挂单减少，只随卖出成交减少。
- `local_sell_frozen` 随卖单挂出增加，随成交、撤单、拒单释放。
- 撤单只减少 `local_sell_frozen`，不增加 `yd_pos`。

### variables 中建议展示

```python
[
    "pos",
    "yd_pos",
    "td_pos",
    "local_sell_frozen",
    "position_synced",
]
```

### 引擎内部建议维护

```python
sell_frozen_by_orderid: dict[str, float]
```

用于记录每个活跃卖单冻结的可卖仓数量。

### T+1 开关配置入口

回测端通过 `ctabacktester` 的全局复选框传入 `BacktestingEngine.set_parameters(t1=...)`。

实盘端也需要有明确入口。建议不要要求每个策略手工把 `t1` 加入自己的 `parameters`，而是在 CTA 基类/引擎层统一支持：

- `CtaTemplate.__init__()` 从策略 `setting` 中读取 `t1`，默认 `False`。
- `CtaEngine.get_strategy_class_parameters()` 返回参数时，统一补充 `t1`。
- `CtaEngine.get_strategy_parameters()` 返回实例参数时，也包含当前 `strategy.t1`。
- `CtaEngine.edit_strategy()` 保存设置时保留 `t1` 字段。

这样旧策略不需要改代码，也能在实盘 UI 中开启 T+1。

## template.py 改造

### CtaTemplate

增加 T+1 参数和持仓变量。

默认：

```python
t1 = False
```

确保旧策略不配置该字段时仍按 T+0 运行。

### short/cover 处理

T+1 模式下：

- `short()` 直接拒绝并写日志。
- `cover()` 直接拒绝并写日志。

示例日志：

```text
T+1模式不支持做空，已拒绝short委托
T+1模式不支持空头平仓，已拒绝cover委托
```

### TargetPosTemplate

当前回测模式中，`pos_change < 0` 会走 `short()`，A股模式下需要改成 `sell()`。

T+1 模式下：

- `target_pos > pos`：买入。
- `target_pos < pos`：卖出。
- 卖出数量不能超过 `yd_pos - local_sell_frozen`。
- 不允许目标仓位小于 0。

## engine.py 实盘改造

### 使用 vnpy_xt 持仓持续同步

`CtaEngine` 需要订阅并处理 `EVENT_POSITION`。

`vnpy_xt` 侧需要保证在查询到持仓或持仓发生变化后，通过 vn.py 标准接口推送持仓：

```python
self.gateway.on_position(position)
```

该调用会由 vn.py 主引擎/event engine 进一步触发标准持仓事件：

```python
EVENT_POSITION
```

`CtaEngine` 的 T+1 持仓同步逻辑只依赖 `EVENT_POSITION`，不直接依赖 `vnpy_xt` 的内部对象。

每次收到 `PositionData` 后，如果匹配策略的 `vt_symbol` 和 `Direction.LONG`，同步：

```python
strategy.pos = position.volume
strategy.yd_pos = position.yd_volume
strategy.td_pos = max(position.volume - position.yd_volume, 0)

strategy.position_synced = True
strategy.position_sync_time = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
```

对于 `vnpy_xt`：

- `PositionData.volume` 对应真实总持仓。
- `PositionData.yd_volume` 可对应 `xt_position.can_use_volume`。
- `PositionData.frozen` 对应冻结数量。
- 持仓查询回调和持仓变化回调都应最终调用 `gateway.on_position(position)`。

### 启动前主动查持仓

T+1 策略启动前，应主动触发一次持仓查询，避免刚启动时 `yd_pos = 0` 导致可卖仓被误拒。

建议增加启动保护：

- T+1 策略启动后，未收到首次 `EVENT_POSITION` 前，不允许卖出。
- 未同步持仓时允许买入，但买入只会增加 `td_pos`，当天不能卖。
- 如果持仓查询超时，应写日志提示“尚未完成真实持仓同步”，而不是静默使用 0 仓。

### 持仓同步策略

采用“持续同步 + 简单保守冻结”的方式：

- 实盘以真实账号可用仓为主。
- 卖单发出成功后，本地临时冻结。
- 收到新的持仓事件后，以真实账号持仓覆盖本地持仓状态。
- `local_sell_frozen` 用于持仓事件刷新前的短窗口，防止重复卖出。

### 下单前校验

T+1 模式下，下单前检查：

1. 是否做空。
2. 数量是否 100 整数倍。
3. 卖出数量是否超过可卖仓。
4. 实盘是否已经同步过持仓。

卖出可用数量：

```python
available = max(strategy.yd_pos - strategy.local_sell_frozen, 0)
```

如果卖出量超过可用量：

- 整单拒绝。
- 返回空列表 `[]`。
- 写日志。

不建议自动截断数量，避免实际交易意图被悄悄改变。

### 卖单冻结

卖单发送成功后：

```python
strategy.local_sell_frozen += volume
sell_frozen_by_orderid[vt_orderid] = volume
```

如果一次策略委托因为 offset converter 被拆成多个实际委托，应按每个实际 `vt_orderid` 分别记录冻结量。

卖单生命周期中的持仓更新口径：

```text
下卖单成功：
    local_sell_frozen += order.volume
    yd_pos 不变

卖单部分成交：
    yd_pos -= traded_increment
    local_sell_frozen -= traded_increment

卖单全部成交：
    yd_pos -= traded_increment
    local_sell_frozen -= traded_increment

卖单撤单：
    local_sell_frozen -= remaining_volume
    yd_pos 不变

卖单拒单：
    local_sell_frozen -= frozen_volume
    yd_pos 不变
```

关键语义：

```text
yd_pos = 总可卖仓
local_sell_frozen = 活跃卖单占用仓
available = yd_pos - local_sell_frozen
```

实现时要注意 `OrderData.traded` 是累计成交量。如果用订单事件释放冻结，需要记录每个订单上一次已处理成交量，按增量计算：

```python
traded_increment = order.traded - previous_traded
```

如果用 `TradeData` 事件更新成交，则每笔 trade 天然是增量，但撤单/拒单时仍需要通过订单终态释放剩余冻结：

```python
remaining_frozen = frozen_volume - processed_traded
```

释放后要确保：

```python
strategy.local_sell_frozen = max(strategy.local_sell_frozen, 0)
strategy.yd_pos = max(strategy.yd_pos, 0)
```

### stop 卖单冻结

T+1 模式下，stop 卖单也会占用可卖仓。建议下 stop 卖单成功时即冻结：

```python
strategy.local_sell_frozen += stop_order.volume
sell_frozen_by_orderid[stop_order.stop_orderid] = stop_order.volume
```

stop 单触发并转成普通委托时，需要把冻结关系从 `stop_orderid` 转移到实际 `vt_orderid`，不能重复冻结：

```text
stop_orderid 冻结 -> vt_orderid 冻结
local_sell_frozen 不变
```

stop 单撤销时释放冻结；stop 单触发后的成交、撤单、拒单按普通委托规则释放。

## backtesting.py 回测改造

### BacktestingEngine 增加参数

`set_parameters()` 增加：

```python
t1: bool = False
```

默认关闭，兼容原 T+0。

### 回测初始持仓

T+1 回测默认：

```python
strategy.pos = 0
strategy.yd_pos = 0
strategy.td_pos = 0
strategy.local_sell_frozen = 0
```

### 日期切换

在 `new_bar()` / `new_tick()` 中检测日期变化。

新交易日第一根数据前结转：

```python
strategy.yd_pos = max(strategy.pos, 0)
strategy.td_pos = 0
strategy.local_sell_frozen = 0
```

### 下单校验

回测中 `send_limit_order()` 和 `send_stop_order()` 均需要 T+1 检查：

- 不允许做空。
- 数量必须是 100 整数倍。
- 卖出不能超过 `yd_pos - local_sell_frozen`。
- stop 卖单也要占用可卖仓。

### 撮合前二次校验

`cross_limit_order()` 和 `cross_stop_order()` 成交前再次检查可卖仓，防止状态变化导致超卖。

### 成交后更新

买入成交：

```python
strategy.pos += volume
strategy.td_pos += volume
```

卖出成交：

```python
strategy.pos -= volume
strategy.yd_pos -= volume
strategy.local_sell_frozen -= volume
```

撤单或拒单不改变 `yd_pos`，只释放对应剩余冻结量：

```python
strategy.local_sell_frozen -= remaining_volume
```

## ctabacktester 改造

### UI 增加开关

在回测参数区域增加复选框：

```text
启用T+1/A股模式
```

默认不勾选。

### 配置保存

保存到 `cta_backtester_setting.json`：

```python
"t1": t1
```

旧配置没有该字段时：

```python
t1 = setting.get("t1", False)
```

### 参数传递链路

```text
BacktestingWidget.start_backtesting()
    -> BacktesterEngine.start_backtesting(..., t1)
        -> BacktesterEngine.run_backtesting(..., t1)
            -> BacktestingEngine.set_parameters(..., t1=t1)
```

参数优化也应传入同一个 `t1` 开关，保证优化和单次回测逻辑一致。

## 拒单日志建议

建议统一日志格式：

```text
T+1模式拒绝做空委托
T+1模式拒绝空头平仓委托
A股模式委托数量必须为100整数倍：当前数量=50
T+1模式可卖仓不足：委托卖出=500，可卖=300
T+1模式尚未完成真实持仓同步，拒绝卖出委托
```

## 需要重点测试的场景

1. T+1 开关关闭，原 T+0 回测结果不变。
2. T+1 开关开启，当天买入后当天卖出被拒绝。
3. T+1 开关开启，昨日仓可以卖出。
4. T+1 开关开启，卖出数量超过可卖仓被拒绝。
5. T+1 开关开启，`short/cover` 被拒绝并打印日志。
6. 买入数量不是 100 整数倍，被拒绝。
7. 卖出数量不是 100 整数倍，被拒绝。
8. stop 卖单不能绕过 T+1 限制。
9. 部分成交后冻结数量正确释放。
10. 撤单后冻结数量正确释放。
11. 拒单后冻结数量正确释放。
12. 回测跨日后，今日仓正确转为昨日仓。
13. 实盘启动后能从 `vnpy_xt` 持仓初始化。
14. 实盘运行中持仓事件能持续同步 `yd_pos`。
15. 持仓事件刷新前，`local_sell_frozen` 能防止重复卖出。
16. 旧配置文件没有 `t1` 字段时正常运行。

## 推荐落地顺序

1. 在 `CtaTemplate` 增加 T+1 变量和展示变量。
2. 在 `BacktestingEngine` 实现 T+1 回测逻辑。
3. 修改 `TargetPosTemplate`，让 A股模式下减仓走 `sell()`。
4. 在 `CtaEngine` 增加实盘持仓持续同步。
5. 在 `CtaEngine` 增加 T+1 下单校验和冻结释放。
6. 在 `ctabacktester` UI 增加 T+1 开关。
7. 补充回测测试用例。
8. 使用 `vnpy_xt` 做实盘模拟/小资金验证。
9. 全部功能开发完成后，增加一个 T+1 示例策略，便于回测和实盘验证。

## T+1 示例策略任务

功能开发完成后，新增一个只做多的 T+1 双均线示例策略。

策略规则：

- 使用 3 日均线和 5 日均线。
- `MA3 > MA5` 时买入。
- `MA3 < MA5` 时卖出。
- 不做空，不调用 `short/cover`。
- 卖出前遵守 T+1 可卖仓限制，即最多只能卖出 `yd_pos - local_sell_frozen`。
- 委托数量使用 100 股整数倍。

建议文件：

```text
vnpy_ctastrategy/strategies/t1_double_ma_strategy.py
```

策略用途：

- 作为 T+1 回测样例。
- 作为 `ctabacktester` 开关验证样例。
- 作为实盘 `vnpy_xt` 持仓同步和卖出限制验证样例。
